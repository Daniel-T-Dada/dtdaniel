rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Helper function to check if user is admin
    function isAdmin() {
      return request.auth != null && request.auth.uid == "1Cd7pndVoZYb84jDwZJFIXbv62s2";
    }

    // Allow read access to all collections
    match /projects/{document=**} {
      allow read: if true;
      allow write: if isAdmin(); // Only allow writes through admin
    }
    
    match /skills/{document=**} {
      allow read: if true;
      allow write: if isAdmin(); // Only allow writes through admin
    }
    
    match /about/{document=**} {
      allow read: if true;
      allow write: if isAdmin(); // Only allow writes through admin
    }
    
    // Allow write access to messages collection for contact form
    match /messages/{document=**} {
      allow read: if isAdmin(); // Only allow reads through admin
      allow create: if true; // Allow users to submit contact messages
      allow update, delete: if isAdmin();
    }

    // Allow authenticated admin users to manage their FCM tokens
    match /adminUsers/{userId} {
      allow read, write: if request.auth != null && request.auth.uid == userId;
    }
  }
} 